name: Happy new year!

on:
  schedule:
    - cron: '0 15 31 12 *' # JST
  workflow_dispatch:

jobs:
  greeting:
    runs-on: ubuntu-latest

    steps:
      - run: year=$(date | awk '{print $NF}') && echo 'Happy new year! '$year
